{
  "scripts": {
    "cy:run": "cypress run --browser chrome --headed"
  },
  "devDependencies": {
    "@cypress/xpath": "^2.0.3",
    "cypress": "^12.9.0",
    "cypress-mochawesome-reporter": "^3.3.0"
  }
}
